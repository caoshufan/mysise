<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devin.client.mysise" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Base" >
        <activity android:name=".ui.activity.WellcomeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.student.StudentMainActivity"
            android:label="@string/app_name"
            android:theme="@style/Root" />
        <activity android:name=".ui.activity.student.DetailSubjectActivity" />
        <activity android:name=".ui.activity.student.ScheduleActivity" />
        <activity android:name=".ui.activity.LoginActivity" />
        <activity android:name=".ui.activity.student.EncouragePunishActivity" />
        <activity android:name=".ui.activity.student.AttendanceActivity" />
        <activity android:name=".ui.activity.student.ExamActivity" />
        <activity android:name=".ui.activity.student.OpenSubjectActivity" />
        <activity android:name=".ui.activity.student.StatusActivity" />
        <activity
            android:name=".ui.activity.teacher.TeacherMainActivity"
            android:label="@string/title_activity_teacher_main"
            android:theme="@style/Base" />
        <activity
            android:name=".ui.activity.teacher.TeacherLoginActivity"
            android:label="@string/title_activity_teacher_login" />
        <activity android:name=".ui.activity.teacher.CoursePlanActivity" >
        </activity>
    </application>

</manifest>
